% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/costsplitter.R
\name{costsplitter}
\alias{costsplitter}
\title{Costs Splitter Function}
\usage{
costsplitter(df, pay_by = "group")
}
\arguments{
\item{df}{A dataframe containing the cost data. The dataframe must have at least the following columns:
\code{name}, \code{group}, \code{age}, columns starting with \code{"share"} indicating the share of costs,
and columns for \code{adjustment} and \code{pay} indicating adjustments and payments.}

\item{pay_by}{A character string specifying whether the costs should be split by \code{"group"} (default) or \code{"individual"}.}
}
\value{
A dataframe with the total amount to pay per group or individual based on the specified criteria.
}
\description{
This function processes a dataset of costs and divides the total costs based on specified criteria.
It calculates the amount each group or individual needs to pay, considering various factors such as
age, share, and adjustments.
}
\examples{
df <- tibble::tibble(
  name = c("Alice", "Bob", "Charlie"),
  group = c("Group1", "Group1", "Group2"),
  age = c("adult", "kid", 30),
  share_meal = c(0.5, 0.3, 0.7),
  share_tour = c(0.4, 0.6, 0.5),
  adjustment = c(1.0, 0.8, 1.2),
  pay_meal = c(100, 50, 150),
  pay_tour = c(200, 100, 250)
)
costsplitter(df, pay_by = "group")
}
